export enum RuleType {
  POLICY = 'policy',
  RESTRICTION = 'restriction',
  PUSH = 'push',
}

export enum RuleFieldType {
  STRING = 'string',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  OBJECT = 'object',
  ARRAY = 'array',
}
